// AWS Credentials (optional - used for fallback AWS services)
const ACCESS_KEY_ID = "";
const SECRET_ACCESS_KEY = "";
const AWS_REGION = "us-east-1";

// Application Information
const APP_NAME = "Database MCP Assistant";
const APP_SUBJECT = "Database Analysis";
const WELCOME_MESSAGE =
  "I'm your Database MCP Assistant, connecting to databases via MCP for data insights.";

// Amazon Bedrock AgentCore Configuration
const AGENT_RUNTIME_ARN = "arn:aws:bedrock-agentcore:us-east-1:761018861641:runtime/db_mcp_assistant-3Ml89SHjQG";
const AGENT_ENDPOINT_NAME = "DEFAULT";
// AgentCore Memory - Retrieve the last K conversation turns for context
const LAST_K_TURNS = 10;

// UI Configuration
const MAX_LENGTH_INPUT_SEARCH = 140;

// Pure MCP approach - no chart generation needed

export {
  APP_NAME,
  APP_SUBJECT,
  WELCOME_MESSAGE,
  AGENT_RUNTIME_ARN,
  AGENT_ENDPOINT_NAME,
  LAST_K_TURNS,
  MAX_LENGTH_INPUT_SEARCH,
  ACCESS_KEY_ID,
  SECRET_ACCESS_KEY,
  AWS_REGION,
};
